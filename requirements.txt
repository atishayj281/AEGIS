fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
pinecone>=5.0.0
boto3>=1.34.0
numpy>=1.26.0
httpx>=0.26.0
openai>=1.10.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
pillow>=10.2.0
pyjwt[cryptography]>=2.8.0
cryptography>=41.0.0
email-validator>=2.1.0  # required by pydantic EmailStr (app/api/admin.py)

# --- Phase 2: Postgres + multi-tenancy (task 2.1) ---
sqlalchemy>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# --- Phase 4: Pinecone vector store + object storage (tasks 4.1, 4.5) ---
# pinecone and boto3 already listed above
langchain-nvidia-ai-endpoints>=0.3.0
langchain-experimental>=0.3.0

# --- Phase 5: Redis + Celery (tasks 5.1, 5.4) ---
redis>=5.0.0
celery>=5.3.0

# --- Phase 6: Compliance & Audit ---
# psycopg2-binary: sync DB driver for Celery retention sweep task
# (asyncpg cannot be used in a synchronous Celery worker context)
psycopg2-binary>=2.9.0
